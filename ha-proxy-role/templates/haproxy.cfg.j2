global
    log /dev/log local0
    log /dev/log local1 notice
    chroot /var/lib/haproxy
    stats socket /run/haproxy/admin.sock mode 660 level admin expose-fd listeners
    stats timeout 30s
    user haproxy
    group haproxy
    daemon

    # Default SSL material locations
    ca-base /etc/ssl/certs
    crt-base /etc/ssl/private

    # SSL config from Mozilla (Intermediate, HAProxy 2.0.3)
    ssl-default-bind-ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384
    ssl-default-bind-ciphersuites TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256
    ssl-default-bind-options ssl-min-ver TLSv1.2 no-tls-tickets

defaults
    log global
    mode tcp
    option tcplog
    timeout connect 5000
    timeout client  50000
    timeout server  50000

# LDAP Write Operations (to Masters)
frontend ldap_write
    bind *:389
    mode tcp
    default_backend ldap_master

backend ldap_master
    mode tcp
    balance leastconn
{% for master in local_masters %}
    server {{ master.inventory_hostname }} {{ master.ldap_host }}:389 check
{% endfor %}

# LDAP Read Operations (to Replicas)
frontend ldap_read
    bind *:390
    mode tcp
    default_backend ldap_replicas

backend ldap_replicas
    mode tcp
    balance roundrobin
{% for reader in local_readers %}
    server {{ reader.inventory_hostname }} {{ reader.ldap_host }}:389 check
{% endfor %}